// Display kiosk loading — plain black, no shimmer (fullscreen kiosk mode)
export default function DisplayLoading() {
  return <div style={{ width: "100vw", height: "100vh", background: "#000" }} />;
}
